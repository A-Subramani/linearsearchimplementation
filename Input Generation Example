int main(int argc, const char * argv[]) {
    // insert code here...
    
    int n, x, i, j;
    int *A;
    int d;
    
    srand(time(NULL));
    

    for(n = 1; n <= 3000; n++){
        A = (int*) malloc(n * sizeof(int));
        for(i = 0; i < n; i++)
            A[i] = i+1;
        
        x = n/2;
    }
    
    return 0;
